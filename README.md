# Data-Viz-Tool

## Frontend - Vite

Install it and run:

```bash
cd React-UI
npm install
npm run dev
```

## Backend - Poetry

Install it and run:

```bash
cd Python-Flask
poetry install
python app/__init__.py
```